import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  content: {
    display: 'flex',
    flexDirection: 'column',
    rowGap: '1.5rem',
  },
  prose: {
    marginTop: '3rem',
  },
});
