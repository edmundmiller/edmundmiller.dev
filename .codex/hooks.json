{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "command": "primary=$(git worktree list --porcelain | sed -n 's/^worktree //p' | head -n 1) && bash \"$primary/scripts/codex-stop-dispatch\" scripts/codex-closeout-hook",
            "timeout": 120,
            "type": "command"
          }
        ]
      }
    ]
  }
}
